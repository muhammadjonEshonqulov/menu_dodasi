name: menu_dodasi
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  chucker_flutter: ^1.3.1
  cached_network_image: ^3.2.1
  flutter_staggered_grid_view: ^0.6.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
  - images/

  fonts:
     - family: Arsenal
       fonts:
         - asset: fonts/arsenal_regular.ttf

